@model WebApplication1.ViewModels.EmployeeListViewModel
@{
    Layout = "~/Views/Shared/MyLayout.cshtml";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
    <script>

    </script>
</head>
<body>
    <h2>@Model.UserName,@Model.Greeting</h2>
    <p>@Html.ActionLink("新增", "CreateEmployee")</p>
    <form action="/employee/QueryEmployeeForName" method="post">
        根据名称查询：<input type="text" name="queryString">
        <input type="submit" value="查询"><input type="reset" value="重置">
    </form>
    <div> 
        <ul>
            @foreach (var item in Model.EmployeeViewList)
            {
                <li>@item.EmployeeName,@item.EmployeeSalary,@item.EmployeeGrade @Html.ActionLink("删除", "delete", new { id = item.EmployeeId }) @Html.ActionLink("修改", "UpdateEmployee", new { id = item.EmployeeId }) </li>
            }
        </ul>
    </div>
    
    <div>
    </div>
</body>
</html>
