@model WebApplication1.ViewModels.EmployeeListViewModel
@{
    Layout = "~/Views/Shared/MyLayout.cshtml";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
    <script>

    </script>
</head>
<body>
    <h2>@Model.UserName,@Model.Greeting</h2>
    <p>@Html.ActionLink("新增", "CreateEmployee")</p>
    <form action="/employee/QueryEmployeeForName" method="post">
        根据名称查询：<input type="text" name="queryString">
        <input type="submit" value="查询"><input type="reset" value="重置">
    </form>
    <div> 
        <ul>
            @foreach (var item in Model.EmployeeViewList)
            {
                <li>@Html.DisplayNameFor(Model => item.EmployeeName): @item.EmployeeName,@Html.DisplayNameFor(Model => item.EmployeeSalary): @item.EmployeeSalary,
            @Html.DisplayNameFor(Model => item.EmployeeGrade): @item.EmployeeGrade @Html.ActionLink("删除", "delete", new { id = item.EmployeeId }) @Html.ActionLink("修改", "UpdateEmployee", new { id = item.EmployeeId }) </li>
            }
        </ul>
    </div>
    
    <di	
When Harry Met Sally	1989/1/11	Romantic Comedy	¥7.99	PG	Edit | Details | Delete
Ghostbustersv>
    </div>
</body>
</html>
